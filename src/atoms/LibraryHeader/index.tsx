import React from 'react';
import { LibraryHeaderContainer } from './library-header-styles';

const LibraryHeader: React.FunctionComponent<any> = () => {
  return (
    <LibraryHeaderContainer>
      Public Library: Online Book Store
    </LibraryHeaderContainer>
  );
};

export default LibraryHeader;
